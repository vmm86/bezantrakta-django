Возможные будущие изменения
===========================

.. todo:: Сделать некоторые страницы, создаваемые в HTML в модели ``Article`` (*"Публичная оферта"*, *"Реклама"*, возможно *"Контакты"*) динамически генерируемыми в приложении ``seo``, чтобы избежать дублирования.

.. todo:: Связать баннеры в модели ``BannerGroupItem`` с контейнерами через модель ``EventContainerBinder`` для того, чтобы получать все элементы в контейнере однои запросом, а не делать 2 запроса для событий и баннеров и объединять их перед выводом в шаблоне.

.. todo:: Разбить приложение ``event`` на отдельные приложения ``event_category``, ``container``, ``event_link``, ``venue``, т.к. их функционал разрастается и становится слишком широким для одного приложения.

.. todo:: Добавить в модель ``EventVenue`` поля ``address`` (адрес), ``latitude`` и ``longitude`` (географические координаты).

.. todo:: Добавить в модель ``BannerItem`` поле ``script`` (скрипт, который может потребоваться при клике на изображение баннера).
